apply plugin: "com.android.application"
apply plugin: "org.jetbrains.kotlin.android"
apply plugin: "com.facebook.react"

/**
 * React Native Gradle Extension configuration
 * This is the new, correct format for React Native 0.73
 */
react {
    // Root directory of your project
    root = file("..")
    
    // Directory where the react-native NPM package is located
    reactNativeDir = file("../node_modules/react-native")
    
    // Directory where the react-native Codegen package is located
    codegenDir = file("../node_modules/@react-native/codegen")
    
    // The cli.js file location
    cliFile = file("../node_modules/react-native/cli.js")
    
    // The entry file for bundle generation
    entryFile = file("../index.js")
    
    // The name of the generated asset file containing your JS bundle
    bundleAssetName = "index.android.bundle"
    
    // Disable creating a separate bundle for each architecture
    enableSeparateBuildPerCPUArchitecture = false
    
    // Enable Proguard in release builds
    enableProguardInReleaseBuilds = true
}

def jscFlavor = 'org.webkit:android-jsc:+'

android {
    // These values are taken from rootProject.ext (set in android/build.gradle)
    ndkVersion rootProject.ext.ndkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion
    compileSdk rootProject.ext.compileSdkVersion

    namespace "com.dip83287.bubbleoverlayapp"
    
    defaultConfig {
        applicationId "com.dip83287.bubbleoverlayapp"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
        multiDexEnabled true
    }

    // Source sets configuration - crucial for manifest merging
    sourceSets {
        main {
            manifest.srcFile 'src/main/AndroidManifest.xml'
            java.srcDirs = ['src/main/java']
            res.srcDirs = ['src/main/res']
            assets.srcDirs = ['src/main/assets']
        }
        debug {
            manifest.srcFile 'src/debug/AndroidManifest.xml'
        }
        release {
            manifest.srcFile 'src/release/AndroidManifest.xml'
        }
    }

    // Packaging options to handle duplicate files
    packagingOptions {
        pickFirst '**/libjsc.so'
        pickFirst '**/libc++_shared.so'
        pickFirst '**/libreact_nativemodule_core.so'
        pickFirst '**/libturbomodulejsijni.so'
        pickFirst '**/libhermes.so'
        pickFirst '**/libjscexecutor.so'
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/license.txt'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/NOTICE.txt'
        exclude 'META-INF/notice.txt'
        exclude 'META-INF/ASL2.0'
        exclude 'META-INF/*.kotlin_module'
    }

    signingConfigs {
        debug {
            storeFile file('debug.keystore')
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            // For production, use your own signing config
            signingConfig signingConfigs.debug
            minifyEnabled enableProguardInReleaseBuilds
            proguardFiles getDefaultProguardFile("proguard-android.txt"), "proguard-rules.pro"
        }
    }
}

dependencies {
    // React Native core dependency
    implementation("com.facebook.react:react-android")
    
    // Multi-dex support
    implementation("androidx.multidex:multidex:2.0.1")
    
    // Hermes or JSC
    if (hermesEnabled.toBoolean()) {
        implementation("com.facebook.react:hermes-android")
    } else {
        implementation jscFlavor
    }
}

// Apply native modules and vector icons
apply from: file("../../node_modules/@react-native-community/cli-platform-android/native_modules.gradle"); applyNativeModulesAppBuildGradle(project)
apply from: file("../../node_modules/react-native-vector-icons/fonts.gradle")
